<%= form_tag(posts_path, method: :get, id: 'search-form') do %>
  <%= text_field_tag :search, params[:search], class: 'search-query', type: 'search', data: { autocomplete: autocomplete_tag_name_posts_path, multiple: true, delimiter: ' '} %>
  <%= submit_tag 'Search' %>
<% end %>
